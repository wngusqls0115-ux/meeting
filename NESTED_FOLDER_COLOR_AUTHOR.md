# Nested Folder + Color + Author v1

## 하위 폴더
- 폴더 행의 `+` 버튼으로 해당 폴더 아래 하위 폴더 생성
- 깊이 제한 없이 계층 구조 지원
- 회의 가져오기/수정/빠른 이동 선택창에서 계층을 들여쓰기로 표시
- 상위 폴더 삭제 시:
  - 그 폴더의 회의록은 한 단계 위 폴더로 이동
  - 하위 폴더도 한 단계 위로 재배치
  - 회의록/하위 폴더 자체는 삭제되지 않음

## 폴더 색상
- 폴더 생성 시 색상 지정
- 각 폴더 행의 색상 선택기로 즉시 변경
- 회의 목록/회의 상세에도 폴더 색상 점 표시

## 작성자
- 회의 등록 시 작성자 입력 가능
- 기본값은 로그인 사용자 이름(없으면 이메일)
- 수정 화면에서 변경 가능
- 회의 목록, 상세 화면, 공유 화면에 작성자 표시

## DB migration
기존 PostgreSQL/SQLite DB에 다음 컬럼을 자동 추가:
- folders.parent_id
- folders.color
- meetings.author
