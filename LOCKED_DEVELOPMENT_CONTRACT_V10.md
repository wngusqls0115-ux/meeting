# MEETING APP DEVELOPMENT CONTRACT — V10

Status: LOCKED_BASELINE

이 파일은 앞으로의 개발에서 기존 확정 기능이 의도치 않게 바뀌는 것을 방지하기 위한 기준입니다.
사용자가 명시적으로 변경을 요청하지 않는 한 아래 사항은 유지합니다.

## 1. 데이터 보존
- Render PostgreSQL `meeting-minutes-db` 유지.
- 기존 meetings / folders / users / follow_up_items 데이터 삭제 금지.
- migration은 additive 방식만 허용.
- DROP TABLE / TRUNCATE TABLE 금지.
- 코드 업데이트/재배포 후 기존 회의록과 폴더가 유지되어야 함.

## 2. 접근 및 로그인
- 앱 링크 자체는 접근 가능.
- 회의록 내용 조회/수정은 로그인 사용자만 가능.
- 저장소 상태/DB backend 확인 UI는 표시하지 않음.
- 로그인 보호 기능은 유지.
- 사용자 비밀번호는 자유양식: 길이/대소문자/숫자/특수문자 조합 제한 없음.
- 빈 비밀번호만 금지.

## 3. 회의 추가 및 회의록 구조
- 좌측 버튼명: `+ 회의 추가`.
- PLAUD 결과는 복사/붙여넣기로 등록 가능.
- 회의록 상세 구조: 회의개요 / 요약 / 회의 세부사항 / F/U 사항.
- 회의개요: 회의명 / 일시 / 회의장소 / 회의방식 / 참석자 / 작성자 / 회의목적.
- 20분 자동저장 유지.

## 4. 폴더
- 하위 폴더 지원.
- 폴더 색상 10개 muted palette 유지.
- 오른쪽 클릭 메뉴: 이름 변경 / 하위폴더 추가 / 색상 / 최상위 이동 / 삭제.
- 폴더명 클릭: 회의록 펼침 / 재클릭: 접힘.
- 하위 회의록은 회의명만 간단 표시.
- 폴더 이동은 맨 왼쪽 `=` 전용 핸들.
- 폴더명 자체는 이동 핸들이 아님.
- 폴더 이동 결과는 DB에 저장.
- 하위 폴더 자체 접기/펼치기 유지.

## 5. 회의록 이동
- 회의록은 폴더 사이 이동 가능.
- 서버 PATCH 후 DB readback 검증.
- frontend 독립 GET readback 2차 검증.
- 검증 실패 시 성공 표시 금지.
- 새로고침/새 세션 후 위치 유지.

## 6. 왼쪽 사이드바 순서
반드시 아래 순서를 유지:
1) 사용자/회의 추가
2) 폴더
3) 선택한 폴더 검색 + 회의록 목록
4) F/U 캘린더
- 좌측 안내창 접기/펼치기 유지.
- 접힘 상태 기억.

## 7. F/U
- 업무내용 / 담당자 / 시작일 / 종료일 / 진행·완료 / 완료일 / 완료사항 / 메모 / 색상.
- F/U 색상 10개 muted palette.
- 월별 캘린더에서 F/U 기간 표시.
- 이번 달 F/U 목록.
- F/U 검색: 업무내용 / 담당자 / 메모 / 완료사항 / 연관 회의명.
- 이번 달 F/U 클릭 -> 메모창.
- 메모 DB 저장.
- 연관 회의로 이동 버튼 유지.
- 완료 F/U 시각적 구분.
- 좌측 F/U 캘린더의 `확대 보기` 또는 월 표시를 클릭하면 오른쪽 본문에 큰 월간 캘린더 표시.
- 큰 캘린더에서 이전 달 / 오늘 / 다음 달 이동, 날짜별 F/U 확인, F/U 메모 열기 가능.

## 8. 번역
- 한국어 원본 유지.
- English / 日本語 번역 컨트롤 유지.

## 9. 개발 원칙
- 신규 요청은 기존 기능 위에 additive하게 구현.
- 위 LOCKED_BASELINE 중 하나를 변경하려면 사용자의 명시적 요청이 필요.

## V13 추가 잠금
- F/U 캘린더에서 연도/월/일 선택으로 원하는 날짜로 즉시 이동 가능.
- 큰 캘린더에서도 연도/월/일 선택으로 날짜 이동 가능.
- F/U 캘린더의 업무를 클릭해 여는 메모창에서 업무별 색상 변경 가능하며 DB에 저장.
- 폴더 이동은 왼쪽 `=` 핸들만 사용하고, 전역 pointer 추적/넓은 drop target/사이드바 자동 스크롤을 유지.
- 폴더 아래 사용 안내 문구는 표시하지 않음.
