# Folder Simple Stable v1

폴더 기능을 오류 격리와 빠른 분류 중심으로 단순화했습니다.

## UX
- 사이드바에서 `+ 새 폴더`를 누르면 즉시 입력창 표시
- Enter로 폴더 생성, Esc로 취소
- 각 사용자 폴더 오른쪽의 `×`로 바로 삭제
- 폴더 삭제 시 회의록은 삭제하지 않고 `미분류`로 이동
- 회의록 카드를 폴더 위로 드래그해서 즉시 이동
- 열린 회의록은 제목 아래 `폴더` 선택창으로 즉시 이동
- 새 회의 가져오기/수정에서도 폴더 선택 가능

## 안정성
- 폴더 API가 실패해도 회의록 앱 전체 부팅이 중단되지 않도록 분리
- 회의록의 폴더 이동은 전체 회의록 PUT 대신 작은 전용 PATCH API 사용
- 폴더 생성/삭제/이동 결과를 toast로 명확히 표시
- 삭제할 폴더에 회의가 있으면 몇 건이 `미분류`로 이동하는지 확인 후 삭제
